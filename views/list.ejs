<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>승뮤니티</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="\main.css">
</head>
<body style="position: relative;">

    <%- include('nav.ejs') %>
    <div style="height:55px;"></div>
    
    <!-- ejs 파일에선 아무데서나 자바스크립트문법 사용 가능  -->
    <%for (let i=0; i<글목록.length; i++){ %>
        <div class="white-bg">
            <div class="list-box">
            <% if(글목록[i].vidName !== undefined){ %>
                <a href="/detail/<%= 글목록[i]._id %>">
                    <img class="list-left" src="/image/<%= 글목록[i].imgName %>" onerror="this.src='/image/dinosaurVid.png'" />
                </a>
            <% } else { %>
                <a href="/detail/<%= 글목록[i]._id %>">
                    <img class="list-left" src="/image/<%= 글목록[i].imgName %>" onerror="this.src='/image/dinosaur.png'" />
                </a>
            <% } %>
                <h4 class="list-right">
                    <a href="/detail/<%= 글목록[i]._id %>"><%= 글목록[i].title %></a>
                    <a href="/edit/<%= 글목록[i]._id %>">✏️</a>
                    <span class="delete" data-id="<%= 글목록[i]._id %>">🗑️</span>
                    <p>작성자: <%= 글목록[i].작성자 %></p>
                    <form action="/chat" method="POST">
                        <button type="submit" class="btn" data-id="<%= 글목록[i].작성자_id %>"
                            data-title="<%= 글목록[i].title %>">작성자랑 채팅하기</button>
                    </form>
                </h4>
            </div>
            
        <p style="clear: both; margin: 0px;"></p>
        </div>      
    <% }%>
    <div>
        <!-- <div class="list-box" style="float: left; width: 20%;">
            <button style="border: none;
            background: rgba(0,0,0,0.3);
            border-radius: 5px; margin-left: 5px;"><a href="/plus"
            style="text-decoration-line: none; color: #fff;
            ">글쓰기✏️</a></button>
        </div> -->


        <div class="page-box">
            <p></p>
            <ion-icon class="back" name="caret-back-outline"></ion-icon>
            <%for (let i=0; i<Math.ceil(글수/5); i++){ %>
                <span style="margin: 3px;" class="page-btn" data-num="<%= i+1 %>"><%= i+1 %></span>
            <% }%>
            <ion-icon class="forward" name="caret-forward-outline"></ion-icon>
        </div>
        <div style="position: absolute; right: 20px; background: rgba(0, 0, 0, 0.2);
        padding: 3px; border-radius: 5px;">
            <a href="/plus" style="text-decoration-line: none; color: #fff;">글쓰기✏️</a></button>
        </div>
        <!--  -->
    </div>
    <div style="text-align: center;">
        <a class="list-loc" style="text-align: center;
        text-decoration: none; color: white;" 
        href="/list/next/<%= 글목록[글목록.length-1]._id %>">다음페이지</a>
    </div>
    <!-- <div>
        <button style="border: none;
        background: rgba(0,0,0,0.3);
        border-radius: 5px; margin-left: 5px;"><a href="/plus"
        style="text-decoration-line: none; color: #fff;
        ">글쓰기✏️</a></button>
    </div> -->
    <!-- <div style="text-align: center;">
        <input type="text" class="search_2" id="ssearch-input" placeholder="글제목으로 검색">
        <button type="submit" id="ssearch"><ion-icon name="search-outline"></ion-icon></button>
    </div> -->
    <div class="ccontainer">
        <div class="search-bar">
            <input type="text" class="search_2" id="ssearch-input" placeholder="글제목으로 검색">
            <button type="submit" id="ssearch"><ion-icon name="search-outline"></ion-icon></button>
        </div>
    </div>

    <!-- <div style="position: absolute; right: 0px; top: 50px;">
        <div class="input-group mb-3" style="margin-bottom: 0px !important; margin: auto ; margin-top: 10px;
        width: 50%; ">
            <input type="text" id="input1" class="form-control" aria-describedby="button-addon2" style="height: 25px;">
            <button class="btn btn-outline-secondary" type="button" id="button-addon2"
            style="height: 25px; background-color: #b8db95;"></button>
        </div>
        <div class="chat-Box" style="background-color: white; width: 50%; height: 130px !important; margin: auto;
        margin-bottom: 10px; margin-top: 0px; border: 1px solid darkgray; overflow-y: scroll;
        background: rgba(255,255,255,0.3);">
            <div id="chatBox">
            </div>
        </div>
    </div> -->

    <!-- <hr>
        <footer>
            <p style="margin-top: 5px; margin-left: 10px;">🎄승주 커무니티🎄</p>
        </footer> -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.2/socket.io.js"></script>
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
    <script>
        console.log('<%= 글목록.length %>')
        for (let i=0; i<'<%= 글목록.length %>'; i++){ 
            document.querySelectorAll('.delete')[i].addEventListener('click', function(e){
                if (confirm('징짜 삭제?')==true){
                    fetch('/delete/?docid=' + e.target.dataset.id, {
                    method : 'DELETE',
                    })
                    .then((r)=>r.text())
                    .then((r)=>{
                        // e.target.parentElement.parentElement.style.display = 'none'
                        location.reload();
                        // confirm('삭제 완료')
                    }) // 삭제버튼 누르면 부모요소 통채로 숨김
                }
                else {
                    return 0
                }
            })
        }
         
        // 두번째 검색기능
        $('#ssearch').click(function(){
            var 입력값 = $('#ssearch-input').val()
            console.log(입력값)
            window.location.replace('/ssearch?value='+입력값)  // url 바꾸는 함수 (get요청변환)
        })

        $('.chat').click(function(e){
            var id = e.target.dataset.id;
            var title = e.target.dataset.title;
            $.post('/chatroom', {당한사람id : id, 채팅방 : title}).then(()=>{
                console.log('성공')
            })
        });
        $('.search_2').keypress(function(e){
          if (e.keyCode===13){
            var 입력값 = $('#ssearch-input').val()
            window.location.replace('/ssearch?value='+입력값) 
          }})


        document.getElementsByClassName('page-btn')['<%= 페이지넘버 %>'-1].classList.add('nowPage');
        $('.page-btn').click(function(e){
            $('.page-btn').removeClass('nowPage')
            var a = $(this)
            console.log(e.target.dataset)
            num = e.target.dataset.num;
            // window.location.replace(`/list/${num}`) 
            window.location.href=`/list/${num}?페이지넘버=`+num;
        });

        var socket = io();

        $('#button-addon2').click(function(){
            socket.emit('user-send', $('#input1').val())
            $('#input1').val(''); 
        })

        $('.input-group').keypress(function(e){
            if (e.keyCode===13){
            socket.emit('user-send', $('#input1').val())
            $('#input1').val(''); 
            }
        })
        socket.on('broadcast', function(data){        
            var chat = `<div class="chatOne"> 🐵${data}<div>`
            // var chat = `<div class="chatOne">${'<%= 채팅사람 %>'}: ${data}<div>`
            $('#chatBox').append(chat)
            var 실제높이 = document.querySelector('.chat-Box').scrollHeight;
            console.log(실제높이)
            $('.chat-Box').scrollTop(실제높이+100); // 여기서 스크롤맨밑으로 (데이터를 수신했을때)
        })

        $('.back').click(function(){
            console.log('<%= 페이지넘버 %>'-1)
            num = '<%= 페이지넘버 %>'-1
            window.location.href=`/list/prev/<%= 글목록[글목록.length-글목록.length]._id %>?pageNum=`+num;
        })
        $('.forward').click(function(){
            num = parseInt('<%= 페이지넘버 %>')+1
            window.location.href=`/list/next/<%= 글목록[글목록.length-1]._id %>?pageNum=`+num;
        })
        // var 실제높이 = document.querySelector('.chat-Box').scrollHeight;
        // console.log(실제높이)
        // $('.chat-Box').scrollTop(실제높이+100); // 여기서 스크롤맨밑으로 (데이터를 수신했을때)
    </script>
    

</body>


</html>